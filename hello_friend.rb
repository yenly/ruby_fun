# puts means "print a line to the terminal"
# gets means "read a line from the terminal"
# strip removes all spaces and newlines from both ends

puts "What is your name?"
name = gets.strip
puts "Hello, " + name + "!"
